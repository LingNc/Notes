1. 环境 使用 ubuntu22.04 迁移到 winserver2022 的 hyperv虚拟机
2. 备份 根据Ai：
	1. 在原系统或者LiveCD进行备份（如果可以进入）
	2. 在LiveCD系统中，先挂载要备份的系统目录到/mnt/root
		```bash
	sudo tar --exclude=proc --exclude=sys --exclude=dev --exclude=tmp --exclude=mnt --exclude=media -czpf /mnt/extra/rootfs.tar.gz -C /mnt/root .
		```
	1. 在原系统中/tmp 目录可以换成想要的任何目录
		```bash
		sudo tar --exclude=proc --exclude=sys --exclude=dev --exclude=tmp --exclude=mnt --exclude=media -czpf /tmp/rootfs.tar.gz -C / .
		```
2. 恢复操作：
3. 先将备份好的rootfs.tar.gz放在一个合适的地方
4. 使用一个任意Linux发行版live版本（有iso镜像即可），挂载为DVD即可，然后进入系统
5. 使用fdisk进行/dev/sda分区的初始化
6. 注意先创建一个EFI分区大概300MB来放引导（例如初始化为 /dev/sda1）初始化为 fat32
	```bash
	sudo mkfs.fat -F32 /dev/sda1
	```
7. 剩下的再建为主分区(/dev/sda2) 初始化为ext4
	```bash
	sudo mkfs.ext4 /dev/sda2
	```
8. 问题：要转移rootfs.tar.gz文件到hyperv虚拟机的话，我们可以采用在win 的 “计算机管理” 里面的磁盘管理，选择右边的更多操作的，创建VDHX文件，然后初始化为NTFS格式的磁盘，将rootfs转移到这个虚拟文件系统中，然后在hyperv中添加这个虚拟硬盘，然后mount即可使用。
9. ![](Pasted%20image%2020250226165217.png)
10. 在Linux中挂载所需要的文件系统到 /mnt/root 进行后续的使用
	 ```bash
	# root
	sudo mkdir -p /mnt/root
	sudo mount /dev/sda2 /mnt/root
	# EFI
	sudo mkdir -p /mnt/root/boot/efi
	sudo mount /dev/sda1 /mnt/root/boot/efi
	```
11. 使用 `sudo tar -xpf /mnt/backup/rootfs.tar.gz -C /mnt/root`  进行还原
12. 更新系统配置fstab：
	1. 自动配置
	```bash
	sudo genfstab -U /mnt/root >> /mnt/root/etc/fstab  # 根据UUID生成挂载配置[2,9](@ref)
	```
	2. 如果没有这个软件可以采用手动更新fstab
	先手动获取sda启动盘的uuid
	```bash
	sudo blkid /dev/sda1
	sudo blkid /dev/sda2
	```
	编辑 `/mnt/target/etc/fstab`（如果存在该文件），将根分区和EIF分区的 UUID 更新为新分区的 UUID  
	 例如，将类似下面的那一行：  
     `UUID=旧UUID / ext4 defaults 0 1`     
  修改为：  
     `UUID=新获取的UUID / ext4 defaults 0 1`     
  保存退出
13. 安装GRUB引导
	1. 绑定目录
		sudo mount --bind /dev /mnt/root/dev   
		sudo mount --bind /proc /mnt/root/proc   
		sudo mount --bind /sys /mnt/root/sys
	2. 进入chroot环境
		sudo chroot /mnt/target /bin/bash
	3. 在 chroot 内安装 GRUB  
	 - 如果你的系统使用的是 Ubuntu/Debian 系统，运行：  
	  ```bash
		grub-install /dev/sda   
		update-grub
		```
	 - 对于其他发行版，命令可能略有不同（例如使用 `grub2-install` 和 `grub2-mkconfig`）
	4. 退出chroot `exit`
	5. 卸载绑定的目录
		```bash
		sudo umount /mnt/root/dev   
		sudo umount /mnt/root/proc   
		sudo umount /mnt/root/sys
		```
14. 卸载分区，并关闭LiveCD系统，然后弹出DVD驱动器
	`sudo umount /mnt/root`
15. 启动虚拟机即可完成迁移过程

16. 有一个后来发现的方法，使用微软提供的 disk2vhd，该软件可以实现从win物理机转为vhd虚拟磁盘放在hyperv中启动 （并不在该教程范围内）
17. 后来发现一个更简单的方法，不过没有验证可能存在很多问题
	1. 在 Linux 物理机上创建磁盘镜像**​
	```bash
	# 查看磁盘设备（例如 /dev/sda）
	sudo fdisk -l
	
	# 克隆整个磁盘到镜像文件（替换 /dev/sda 为实际磁盘）
	sudo dd if=/dev/sda of=physical-disk.img bs=4M status=progress
	
	# 压缩镜像（可选）
	gzip physical-disk.img
	```
	2. 将镜像转换为 Hyper-V 格式（VHDX）​**​
	
	```bash
	# 安装 qemu-img（Debian/Ubuntu）
	sudo apt install qemu-utils
	
	# 转换原始镜像为 VHDX（如果已压缩，先解压）
	gunzip physical-disk.img.gz
	qemu-img convert -p -f raw -O vhdx physical-disk.img hyperv-disk.vhdx
	```
	3. 将 VHDX 文件传输到 Hyper-V 主机**​
	```bash
	# 从 Linux 上传到 Windows（需提前开启 Windows 的 SSH 服务）
	scp hyperv-disk.vhdx user@hyperv-host:C:\VMs\
	
	# 或使用 rsync（更高效）
	rsync -avzP hyperv-disk.vhdx user@hyperv-host:C:\VMs\
	```
	4. 在 Hyper-V 中创建虚拟机**​
		1. ​**打开 Hyper-V 管理器**​：  
		    Windows 搜索 → 输入 "Hyper-V 管理器" → 打开。
		2. ​**新建虚拟机**​：
		    - 右键 Hyper-V 主机 → 新建 → 虚拟机。
		    - 名称：`Linux-VM` → 选择存储位置（如 `C:\VMs`）。
		    - 代系：选择 ​**第二代**​（UEFI 启动）。
		    - 内存：分配足够 RAM（如 4096 MB）。
		    - 网络：选择虚拟交换机。
		    - 磁盘：选择 ​**使用现有虚拟硬盘**​ → 浏览到 `hyperv-disk.vhdx`。
		3. ​**启动虚拟机**​：  
		    右键虚拟机 → 启动。
	
	5. 安装 Hyper-V Linux 集成服务（LIS）​
	
	```bash
	# Ubuntu/Debian
	sudo apt update
	sudo apt install linux-azure
	
	# CentOS/RHEL
	sudo yum install hyperv-daemons
	
	# 重启系统
	sudo reboot
	```
	6. 修复引导（如有必要）​**​
	
	```bash
	# 进入恢复模式或 Live CD 环境
	sudo mount /dev/sda1 /mnt
	sudo chroot /mnt
	
	# 重新安装 GRUB 到虚拟磁盘
	grub-install /dev/sda
	update-grub
	```
	
	---
	
	#### ​**三、验证与优化**​
	
	7. ​**检查网络**​：
	    
	    bash
	    
	    复制
	    
	    ```bash
	    ip a  # 确认网卡（eth0）已获取 IP
	    ping 8.8.8.8
	    ```
	    
	8. ​**优化磁盘性能**​：
	    - 在 Hyper-V 设置中将磁盘控制器改为 ​**SCSI**。
	    - 启用 ​**写入缓存**​（虚拟机设置 → SCSI 控制器 → 启用写入缓存）。
	9. ​**调整 CPU/Memory**​：
	    - 根据负载动态分配 vCPU 和内存资源。