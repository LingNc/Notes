1. 手动下载配置，教程： https://blog.iswiftai.com/posts/clash-linux/
2. clash文件： 
	1. 这个好像不能使用tun模式![Clash-Linux-amd64-v1.14.0](clash-linux-amd64-v1.14.0.gz)
	2. 从clash for windows的linux版中提取出的clash二进制可执行文件,该版本可以使用tun模式 ![clash-linux](assert/clash-linux.bin)
3. 如果使用gz压缩文件，接下来使用 `gunzip` 命令解压，并重命名为 `clash`
```bash
gunzip clash-linux-amd64-v1.14.0.gz
mv clash-linux-amd64-v1.14.0 clash
```
4. 下载 `Country.mmdb` 文件地址： https://github.com/Dreamacro/maxmind-geoip/releases
5. 这里是已经整理好的 `Country.mmdb`文件，使用的时候请修改名字为`Country.mmdb` : ![Country_old.mmdb](Country_old.mmdb)![](GeoIP_new.mmdb)
6. 将`clash` 可执行文件赋值到 `/usr/local/bin/clash` 中
7. 在`opt/clash`创建文件夹,将`Country.mmdb`和`config.yaml`放进去
	1. 指令
		```bash
		sudo mkdir /opt/clash
		# 将 clash 文件包括 country文件，还有 config.yaml 准备好放进去
		# sudo cp clash /opt/clash/
		# 采用连接的形式
		sudo ln -s /opt/clash/clash /usr/local/bin/clash
		# 链接之后要添加运行权限
		sudo chmod +x /usr/local/bin/clash
		# 复制配置文件
		sudo cp config.yaml /opt/clash/
		sudo cp Country.mmdb /opt/clash/
		```
	2. 创建 systemd 服务配置文件 
		1. `sudo vim /etc/systemd/system/clash.service`
		```bash
		[Unit]
		Description=Clash daemon, A rule-based proxy in Go.
		After=network.target
		
		[Service]
		Type=simple
		Restart=always
		CapabilityBoundingSet=CAP_NET_ADMIN CAP_NET_BIND_SERVICE CAP_NET_RAW
		AmbientCapabilities=CAP_NET_ADMIN CAP_NET_BIND_SERVICE CAP_NET_RAW
		ExecStart=/usr/local/bin/clash -d /opt/clash
		
		[Install]
		WantedBy=multi-user.target
		```
	3. `config.yaml`模板
		```yaml
		# 基础配置
		mixed-port: 12345
		authentication:
		  - "用户名1:密码1"
		  - "用户名2:密码2"
		# 设置为 true 以允许来自其他 LAN IP 地址的连接
		allow-lan: true
		mode: Rule
		log-level: info
		external-controller: 127.0.0.1:9090
		
		# TUN模式配置 DNS
		dns:
		  enable: true
		  ipv6: true # true/false 是否启用 ipv6 支持
		  # 从 v0.18.8 版本开始，TUN 模式建议使用 fake-ip 模式，redir-host 将无法进行远端 DNS 解析
		  enhanced-mode: fake-ip # redir-host/fake-ip
		  # use-hosts: true # 查询 hosts 并返回 IP 记录
		  default-nameserver: # 用于 DoH/DoT 的 Bootstrap Server
		    - 59.70.159.10 # zzuliDNS
		    - 223.5.5.5  # 阿里公共 DNS
		    - 223.6.6.6  # 阿里公共 DNS
		    - 119.29.29.29 # DNSPOD 公共 DNS
		
		  fake-ip-range: 198.18.0.1/16 # Fake IP 地址池 (CIDR 形式)
		  fake-ip-filter: # 微软系 APP 无法登陆使用等问题，通过添加 fake-ip-filter 解决
		    # === Local ===
		    - "*.lan"
		    - "*.local"
		    # === Microsoft Windows Serivice ===
		    - "*.msftncsi.com"
		    - "*.msftconnecttest.com"
		  nameserver: # GeoIP 为 CN 时使用的 DNS NameServer（使用DoH/DoT）
		    - 59.70.159.10 # zzuliDNS
		    - https://doh.pub/dns-query   # DNSPod DoH
		    - https://dns.alidns.com/dns-query  # 阿里 DoH
		    # - https://[2400:3200::1]/dns-query  # 阿里 DoH
		    # - https://[2400:3200:baba::1]/dns-query # 阿里 DoH
		  fallback: # GeoIP 不是 CN 时使用的 DNS NameServer（使用DoH/DoT）
		    - 59.70.159.10 # zzuliDNS
		    #- https://doh.dns.sb/dns-query    # DNS.SB DoH
		    - https://dns.google/dns-query    # Google DoH
		    - https://1.1.1.1/dns-query   # Cloudflare DoH
		    #- https://1.0.0.1/dns-query   # Cloudflare DoH
		  fallback-filter:
		    geoip: true # 启用 GeoIP
		    ip-cidr:
		      - 240.0.0.0/4
		      - 127.0.0.1/8
		      - 0.0.0.0/32
		    domain:
		      - +.google.com
		      - +.facebook.com
		      - +.twitter.com
		      - +.youtube.com
		      - +.xn--ngstr-lra8j.com
		      - +.google.cn
		      - +.googleapis.cn
		      - +.googleapis.com
		      - +.gvt1.com
		# interface-name: Ethernet  # 出口网卡名称（已注释），建议使用自动检测出口网卡模式👇
		# Tun 配置
		tun:  
		  enable: true  # 启用 Tun 模式
		# 使用 system statck 需要 Clash Premium 2021.05.08 及更高版本
		  stack: system  # gvisor/system 使用 system stack 请按照本文后面防火墙放行程序
		  dns-hijack:
		    - 198.18.0.2:53  # 本地劫持 DNS 地址，无需修改
		  auto-route: true
		  auto-detect-interface: true  # 自动检测出口网卡
		
		# 服务商配置
		# 请自行配置可用的proxy
		proxies:
		# socks5 
			# 名字
		  - name: "Test" 
			# 连接方式    
			type: socks5
			# 地址
		    server: xxxx.xxx
			# 端口
		    port: 7890
		    # username: username 
		    # password: password 
		    # tls: true 
		    # skip-cert-verify: true 
		    # udp: true # http 
		# http
		  - name: "http" 
			type: http 
			server: server 
			port: 443 
			# username: username 
			# password: password 
			# tls: true # https 
			# skip-cert-verify: true 
			# sni: custom.com
		    
		# 代理组
		proxy-groups:
		# 名字
		- name: Select
		  # 类型	
		  type: select
		  # 拥有的代理选项
		  proxies:
		    - XXXXXX
		    - DIRECT # 直连
		
		# 规则可自行参考clash文档
		rules:
		  - DOMAIN-SUFFIX,xxxx.com,DIRECT
		  # 可以添加本地过滤规则
		  - IP-CIDR,10.0.0.0/8,DIRECT
		  - IP-CIDR,172.16.0.0/12,DIRECT
		  # 剩余的全匹配选择某个代理组
		  - MATCH,Select
		```
	4. 如果只是作为一个跳板进行网络代理的服务器，那么可以简易的配置 
		```yaml
		# 基础配置
		mixed-port: 7890
		authentication:
		  - "username:password"
		# 设置为 true 以允许来自其他 LAN IP 地址的连接
		allow-lan: true
		mode: Rule
		log-level: info
		external-controller: 127.0.0.1:9090
		```
	5. 更详细的配置教程参考： https://clash.wiki/
8. 使用快捷仓库clash-tun快速配置clash-tun模式： https://github.com/blue7wings/clash-tun (该仓库有点不太可用)
9. 快捷仓库2: https://github.com/nelvko/clash-for-linux-install?tab=readme-ov-file