1. 安装打开官网根据提示下载，若使用Linux(Debian12)那个命令有一点问题，并且不太安全
	修改后
	
```bash
curl -s 'https://raw.githubusercontent.com/zerotier/ZeroTierOne/main/doc/contact%40zerotier.com.gpg' | gpg --import && \
if z=$(curl -s 'https://install.zerotier.com/'); then echo "$z" | sudo bash; fi
```
	
2. 