1. 基于RemoteApp Tool 通过remoteapp发布应用程序 https://www.iplaysoft.com/remoteapp-tool.html
2. 通过remoteapp发布应用程序（win server 操作） https://zhuanlan.zhihu.com/p/619232980?utm_psn=1866227386724532224
3. 付费文章 讲述如何发布remoteapp虚拟应用（remoteapp tool）  https://www.rockylinux.cn/notes/windows-deploys-virtual-applications-based-on-remoteapp-tool.html
4. 非winserver
在 Windows 10 上通过 RemoteApp 发布应用程序并直接通过“工作区”方式添加（无需生成 RDP 文件），虽然原生的 RemoteApp 功能主要依赖 Windows Server，但可以通过以下方法实现非服务器环境下的类似功能：

---

### **方法一：通过控制面板配置 RemoteApp 工作区**
此方法需要管理员提供工作区 URL（如企业或学校的 RemoteApp 服务），适用于已部署 RemoteApp 服务的场景：
1. **安装根证书**（若需）：
    - 若连接时出现证书错误，需从管理员处获取并安装根证书（如 `ms.local` 证书）。
2. **配置工作区**：
    - 打开控制面板 → **RemoteApp 和桌面连接** → **访问 RemoteApp 和桌面**。
    - 输入管理员提供的 **工作区 URL**（如 `https://rds.example.com/RDWeb/Feed/webfeed.aspx`）。
    - 使用 Office 365 或域账户登录，系统会自动同步发布的 RemoteApp 程序到本地开始菜单。

---

### **方法二：使用第三方工具 RemoteApp Tool**
若需自行在 Windows 10 上搭建 RemoteApp 服务并支持工作区模式，可结合 **RemoteApp Tool** 和 **RDPWrap** 实现：
1. **启用远程桌面服务**：
    - 安装 **RDPWrap**：
     - 下载并解压 [RDPWrap](https://github.com/stascorp/rdpwrap)，以管理员权限运行 `install.bat`。
     - 替换 `rdpwrap.ini` 为最新版本以确保兼容性（通过 CMD 输入 `ver` 获取系统版本号并匹配配置文件）。
    - 运行 `RDPConf.exe`，确保所有状态显示为绿色（表示多用户支持已启用）。
2. **发布 RemoteApp 程序**：
    - 使用 **RemoteApp Tool**：
     - 下载并运行 [RemoteApp Tool](https://github.com/kimmknight/remoteapptool)，以管理员权限启动。
     - 点击 `+` 添加应用程序（如记事本、微信等），设置图标和名称。
     - 在 **“Create Client Connection”** 中生成 RDP 文件时，勾选 **“Use Remote Desktop Gateway”**（若需通过网关访问）。
3. **配置工作区**：
  - 使用RAWeb工具来实现工作区  
     https://github.com/kimmknight/raweb
    新建网页 iss服务管理中 
    iis需要开启web管理的可选功能
    （可能的问题：W3SVC未找到：在 IIS 中创建新网站后显示红色叉并提示 **“在计算机 '.' 未找到服务 W3SVC”**，通常是由于 IIS 核心服务未正确安装或运行异常导致的。以下是针对此问题的分步解决方案：
    **1. 确认 IIS 相关服务已安装**W3SVC（World Wide Web Publishing Service）是 IIS 的核心服务，需确保其已通过 **Windows 功能** 安装：
    1. 打开 **控制面板 > 程序 > 启用或关闭 Windows 功能**。
    2. 展开 **Internet 信息服务**，勾选以下关键组件：- **Web 管理工具**（包含 IIS 管理控制台）。    - **万维网服务**（必须勾选所有子项，尤其是 **应用程序开发功能** 和 **安全性**）。   
    3. 点击 **确定**，等待安装完成并重启电脑5711。）
    http需要开启 万维网服务的功能，记得所有子项都需要打开
    （注意新增win功能之后要重启电脑，不重启之后会有很多问题）
### 安装到 IIS
一。自动安装
1. 下载并解压 RAWeb 的 ZIP 文件。
2. 以管理员powershell运行 setup.ps1 进行安装，一路Y即可（自动开启https自签名）  
3. 此时文件会生成在 C盘中的 inetpub中的RAWeb这里
4. 打开iis服务管理，将C:/inetpub/RAWeb作为根目录，端口随意，开启https的签名，证书可以选择自签名证书，创建服务器
5. 然后讲 auth 子文件夹的身份验证设为 禁止匿名，并且启用windows身份验证
6. 启动服务器即可
7. 在其他的地方添加的工作区的url为 https://ip:port/webfeed.aspx  默认路径为 https://ip:port/default.aspx
二。 手动安装
1. 下载并解压 RAWeb 的 ZIP 文件。
2. 把 aspx/wwwroot 移动到合适的地方，并重命名为 RAWeb 
3. 然后再iis中添加网站站点，将 这个 RAWeb作为根目录，此时应该没有http因为我也不会，也可能使用https然后选择一个证书即可。
    - 在 `auth` 子文件夹（也要转换为应用程序）打开选项中的iis选项卡中的身份验证，禁用 **匿名认证**，启用 **Windows 认证**，重启网页；（问题：如果遇到选项卡中只有一个选项而不是很多或者**HTTP 403.14 错误**，并且身份验证选项中只有 **ASP.NET 模拟**，说明 Windows 身份验证模块未安装，应该是新增万维网的功能后没有重启电脑（iis服务））![[Pasted image 20250125003322.png]]
        
    - 调整应用池的 .NET 版本（推荐使用 .NET 4.0 或更高版本）27。
（问题：如果遇到网页可以打开，但是移动端无法加入工作区，有两种情况 1.权限没有配置好，auth文件夹需要禁用匿名，启用windows用户验证，2.没有把wwwroot（RAWeb）设为网站的根目录路径，导致无法正确识别到（具体原因不清楚））
 最后，注意工作区的链接url为 https://ip:port/webfeed.aspx
 
### **方法三：安装Server Manager来实现win server的功能
 - 安装 server manager（安装后无法管理win10，只能管理远程的win server，版本不支持，应该是系统不支持进行服务器操作）此方案不可行
     https://www.yundongfang.com/Yun288181.html

---
5. remoteapp tool 工具的使用教程 https://github.com/kimmknight/remoteapptool/wiki/Create-a-RemoteApp-and-use-it-on-another-computer