1. drcom的客户端软件，rust重写： https://github.com/zombie110year/drcom.rs/releases/tag/v0.1.4-a1
2. 抓包认证地址适用于 5.2.0(D)和P版:
	1. Linux 的客户端 Ver 1.0.0 (D) : https://blog.csdn.net/zenglinshan/article/details/49928305
	2. 上面的和学校的客户端类似，都是提供一个桌面版的登录认证服务，而不是通过命令行进行进程守护
	3. wiki drcom，链接教学： https://wiki.archlinuxcn.org/wiki/Drcom
	4. 各种版本的 drcom配置，仓库的wiki有各种学校的使用教程 ： https://github.com/drcoms/drcom-generic/tree/master
	5. 不使用学校的客户端，使用第三方的比较常用的drcom客户段: dogcom
		1. 可以通过载入配置文件使用: https://github.com/mchome/dogcom
		2. 克隆仓库后使用`make`进行构建
		3. 或者使用编译好的可执行文件(linux)： ![](dogcom-cli.bin)
		4. 使用教程
			```
			Usage:
			        dogcom -m <dhcp/pppoe> -c <FILEPATH> [options <argument>]...

			Options:
			        --mode <dhcp/pppoe>, -m <dhcp/pppoe>  set your dogcom mode
			        --conf <FILEPATH>, -c <FILEPATH>      import configuration file
			        --bindip <IPADDR>, -b <IPADDR>        bind your ip address(default is 0.0.0.0)
			        --log <LOGPATH>, -l <LOGPATH>         specify log file
			        --802.1x, -x                          enable 802.1x
			        --daemon, -d                          设置为后台进程运行
			        --eternal, -e                         设置为断开后尝试重新（5次）
			        --verbose, -v                         展示日志信息
			        --help, -h                            display this help
			```
			#### 示例:
			```shell
			$ dogcom -m dhcp -c dogcom.conf
			$ dogcom -m dhcp -c dogcom.conf -l /tmp/dogcom.log -v
			$ dogcom -m dhcp -c dogcom.conf -d # (PS: only on Linux build)
			$ dogcom -m pppoe -c dogcom.conf -x # (PS: only on Linux build)
			$ dogcom -m pppoe -c dogcom.conf -e # eternal dogcoming (default times is 5)
			$ dogcom -m pppoe -c dogcom.conf -v
			$ dogcom -m dhcp -c dogcom.conf -b 10.2.3.12 -v
			```
			#### 构建脚本:
			```shell
			$ make # Linux
			$ make win32=y # Windows(MinGW)
			$ make test=y # For testing purposes
			$ make force_encrypt=y # Force open encrypt mode in PPPoE version
			```
	6. 抓包使用WireShark抓取并保存为截包文件
	7. 在线解析截包文件 : https://drcoms.github.io/drcom-generic/
		1. 本地解析截包文件的 py2和py3脚本：
			- ![](drcom_d_config_py2.py)
			- ![](drcom_d_config_py3.py)
		2. 运行上述代码之后会输出一个config.txt文件，里面就是该环境下对应的配置文件
	8. 最后使用的是修改解析出来的文件，设置账号密码，和`host_anme`即可（注意hostname最好自己设置为Windows 10或者其他，他默认是NOTE7）
	9. 在zzuli坏境下，工程楼331机房的配置文件：
		1. 参考示例:
			```conf
			server = '10.30.1.19'
			username = ''
			password = ''
			CONTROLCHECKSTATUS = '\x20'
			ADAPTERNUM = '\x04'
			host_ip = '10.xx.xx.xx'
			IPDOG = '\x01'
			host_name = ''
			PRIMARY_DNS = '59.70.159.10'
			dhcp_server = '0.0.0.0'
			AUTH_VERSION = '\x0f\x00'
			mac = 0x00000xxxxxx
			host_os = 'Windows 10'
			KEEP_ALIVE_VERSION = '\xdc\x02'
			ror_version = False
			```
		2. ![参考文件](drcom-10.30.1.19.conf)
		3. ![新的参考文件202509](drcom_10.30.1.19_33120250911.conf)
	10. 验证登录的脚本，也是可以进行正常登录的脚本 :
		1. py2脚本![](latest-wired.py)
		2. py3脚本![](latest-wired-python3.py)
	11. 客户端会定期发送心跳包：学校有一个专门用来认证的服务器，每个机房或者区域的这个认证服务器可能都不太一样，然后会通过像这个服务器发送认证请求获得上网的权限就可以了，然后定时的发送一个被加密过的心跳包，那边可以验证这个心跳包是按照要求发出的，他识别到你发的是不对的就给你下线了
3. 6.0.0可以通过添加运营商账号（或者学校的账号后缀@cmcc)来PPPOE拨号上网
4. 其他版本参考上述：drcom-generic仓库中的教程
5. 无线网的认证在 探姬写的 郑州轻工业认证上网的python脚本：
https://github.com/ProbiusOfficial/zzuliNetWorkSetUp/tree/main