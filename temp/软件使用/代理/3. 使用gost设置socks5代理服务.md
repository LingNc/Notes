1. 创建日期： 2025-11-19
2. 测试环境： win10/Debian13
3. 参考教程：
	1.
4. 下载地址： https://github.com/ginuerzh/gost/releases v3版本
5. 配置文件（为了避免制表符等缩进问题可以下载文件：[config.yaml](config.yaml)）
	```yaml
	services:
	  # ==========================================
	  # 1. Shadowsocks 服务 (推荐用于公网)
	  # ==========================================
	  # SS - TCP 监听
	  - name: ss-tcp
	    addr: :9000
	    handler:
	      type: ss
	      auth:
	        username: chacha20-ietf-poly1305  # 加密方式
	        password: MyStrongSSPassword  # 【请修改】SS的连接密码
	    listener:
	      type: tcp

	  # SS - UDP 监听 (与上面端口一致)
	  - name: ss-udp
	    addr: :9000
	    handler:
	      type: ss
	      auth:
	        username: chacha20-ietf-poly1305  # 加密方式
	        password: MyStrongSSPassword  # 【请修改】必须和上面完全一致
	    listener:
	      type: udp

	  # ==========================================
	  # 2. SOCKS5 服务 (推荐用于局域网)
	  # ==========================================
	  - name: socks5-service
	    addr: :9001
	    handler:
	      type: socks5
	      auth:
	        username: myuser      # 【请修改】SOCKS5 用户名
	        password: mypassword  # 【请修改】SOCKS5 密码
	    listener:
	      type: tcp
	```
6. win创建服务脚本（将脚本放在gost.exe这个目录中配置文件命名为config.yaml放在同一个文件夹就可以，）
	```powershell
	# =================配置区域=================
	$ServiceName = "GostProxy"                # 服务名称
	$DisplayName = "Gost Proxy Service"    # 显示名称
	$Description = "Gost 隧道服务 - 自动重启保护" # 服务描述
	# =========================================

	# 检查管理员权限
	if (!([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole] "Administrator")) {
	    Write-Warning "请右键点击本脚本，选择【使用 PowerShell 运行】来获取管理员权限！"
	    Start-Sleep -s 5
	    Exit
	}

	# 获取当前脚本所在目录（绝对路径）
	$ScriptPath = $PSScriptRoot
	$NssmPath = Join-Path $ScriptPath "nssm.exe"
	$GostPath = Join-Path $ScriptPath "gost.exe"
	$ConfigPath = "config.yaml" # 相对路径即可，因为我们会设置 AppDirectory

	# 检查文件是否存在
	if (!(Test-Path $NssmPath)) { Write-Error "找不到 nssm.exe，请确保它和脚本在同一目录！"; Exit }
	if (!(Test-Path $GostPath)) { Write-Error "找不到 gost.exe，请确保它和脚本在同一目录！"; Exit }

	Write-Host "正在安装服务: $ServiceName ..." -ForegroundColor Cyan

	# 1. 停止并删除旧服务（如果存在）
	& $NssmPath stop $ServiceName 2>$null
	& $NssmPath remove $ServiceName confirm 2>$null

	# 2. 安装服务
	& $NssmPath install $ServiceName $GostPath

	# 3. 设置启动参数 (-C config.yaml)
	& $NssmPath set $ServiceName AppParameters "-C $ConfigPath"

	# 4. 设置工作目录 (这一步至关重要，否则读不到 config.yaml)
	& $NssmPath set $ServiceName AppDirectory $ScriptPath

	# 5. 设置服务详情
	& $NssmPath set $ServiceName DisplayName $DisplayName
	& $NssmPath set $ServiceName Description $Description
	& $NssmPath set $ServiceName Start SERVICE_AUTO_START  # 开机自动启动

	# 6. 设置崩溃自动重启策略 (核心需求)
	# 设置为：如果程序挂了，无条件重启
	& $NssmPath set $ServiceName AppExit Default Restart
	# 设置重启延迟（防止疯狂重启卡死系统，设置为 3秒）
	& $NssmPath set $ServiceName AppThrottle 3000

	# 7. 启动服务
	Write-Host "正在启动服务..." -ForegroundColor Cyan
	& $NssmPath start $ServiceName

	# 8. 检查状态
	$ServiceStatus = Get-Service -Name $ServiceName
	if ($ServiceStatus.Status -eq 'Running') {
	    Write-Host "✅ 成功！服务已安装并正在运行。" -ForegroundColor Green
	    Write-Host "   - 开机自启：已开启"
	    Write-Host "   - 崩溃重启：已开启"
	} else {
	    Write-Host "❌ 服务安装完成，但启动失败。请检查 config.yaml 是否正确。" -ForegroundColor Red
	}

	Read-Host "按回车键退出..."
	```
7. Linux创建为systemd脚本（和win操作一样放在和gost同一个名录下
	```bash
	#!/bin/bash

	# ================= 配置区域 =================
	SERVICE_NAME="gost"
	# 获取当前脚本所在的绝对路径
	WORK_DIR=$(cd "$(dirname "$0")"; pwd)
	GOST_BIN="$WORK_DIR/gost"
	CONFIG_FILE="$WORK_DIR/config.yaml"
	# ===========================================

	# 颜色定义
	GREEN='\033[0;32m'
	RED='\033[0;31m'
	NC='\033[0m' # No Color

	# 1. 检查 Root 权限
	if [ "$EUID" -ne 0 ]; then
	  echo -e "${RED}错误: 请使用 root 权限运行此脚本 (sudo ./install.sh)${NC}"
	  exit 1
	fi

	# 2. 检查文件是否存在
	if [ ! -f "$GOST_BIN" ]; then
	  echo -e "${RED}错误: 未找到 gost 文件！请确保它在当前目录下。${NC}"
	  echo "路径检查: $GOST_BIN"
	  exit 1
	fi

	if [ ! -f "$CONFIG_FILE" ]; then
	  echo -e "${RED}错误: 未找到 config.yaml 文件！${NC}"
	  exit 1
	fi

	# 3. 赋予执行权限
	chmod +x "$GOST_BIN"
	echo -e "已赋予 gost 执行权限..."

	# 4. 生成 Systemd 服务文件
	SERVICE_FILE="/etc/systemd/system/${SERVICE_NAME}.service"

	echo -e "正在创建服务文件: $SERVICE_FILE ..."

	cat > "$SERVICE_FILE" <<EOF
	[Unit]
	Description=Gost V3 Proxy Service
	After=network.target

	[Service]
	Type=simple
	# 关键：设置工作目录，确保能读取到 config.yaml
	WorkingDirectory=${WORK_DIR}
	ExecStart=${GOST_BIN} -C config.yaml
	# 崩溃自动重启配置
	Restart=always
	RestartSec=3
	# 建议增加文件描述符限制，防止连接数过多报错
	LimitNOFILE=65535

	[Install]
	WantedBy=multi-user.target
	EOF

	# 5. 启动服务
	echo -e "正在加载并启动服务..."
	systemctl daemon-reload
	systemctl enable "$SERVICE_NAME"
	systemctl restart "$SERVICE_NAME"

	# 6. 检查状态
	sleep 2
	if systemctl is-active --quiet "$SERVICE_NAME"; then
	    echo -e "${GREEN}=========================================${NC}"
	    echo -e "${GREEN}✅ 安装成功！Gost 已在后台运行并设置开机自启。${NC}"
	    echo -e "${GREEN}=========================================${NC}"
	    echo -e "查看运行状态: systemctl status $SERVICE_NAME"
	    echo -e "查看实时日志: journalctl -u $SERVICE_NAME -f"
	else
	    echo -e "${RED}❌ 安装完成但启动失败。请查看日志排查原因：${NC}"
	    journalctl -u "$SERVICE_NAME" --no-pager -n 10
	fi
	```