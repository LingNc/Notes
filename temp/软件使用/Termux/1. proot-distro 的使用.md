1. proot-distro版本 v4.25.0
2. 简单使用 使用 proot-distro help获得帮助
	1. 帮助文档 ![](Screenshot_2025-05-03-20-10-54-384_com.termux.png)
	```shell
	Usage: proot-distro [COMMAND] [ARGUMENTS]

	PRoot-Distro is a Bash script wrapper for PRoot. It provides
	a set of functions with standardized command line interface
	to let user easily manage Linux PRoot containers. By default
	it supports a number of well known Linux distributions such
	Alpine Linux, Debian or openSUSE. However it is possible to
	add others with a help of plug-ins.
	
	List of the available commands:
	
	  help         - Show this help information.
	
	  backup       - Backup a specified distribution.
	
	  install      - Install a specified distribution.
	
	  list         - List supported distributions and their
	                 installation status.
	
	  login        - Start login shell for the specified distribution.
	
	  remove       - Delete a specified distribution.
	                 WARNING: this command destroys data!
	
	  rename       - Rename installed distribution.
	
	  reset        - Reinstall from scratch a specified distribution.
	                 WARNING: this command destroys data!
	
	  restore      - Restore a specified distribution.
	                 WARNING: this command destroys data!
	
	  clear-cache  - Clear cache of downloaded files.
	
	  copy         - Copy files from/to distribution.
	
	Each of commands has its own help information. To view it, just
	supply a '--help' argument to chosen command.
	
	Hint: type command 'proot-distro list' to get a list of the
	supported distributions. Pick a distro alias and run the next
	command to install it: proot-distro install <alias>
	
	Runtime data is stored at this location:
	
	  /data/data/com.termux/files/usr/var/lib/proot-distro
	
	If you have issues with proot during installation or login, try
	to set 'PROOT_NO_SECCOMP=1' environment variable.
	
	Proot-Distro v4.25.0 by Termux (@sylirre).
	```
3. 使用 proot-distro [command] --help 获取对应指令的帮助
	2. install 
	![](Screenshot_2025-05-03-20-18-08-470_com.termux.png)
	3. 由于默认的发型版是如果要安装第二个相同的发行版 ，例如已经安装 debian
	4. 可以使用 proot-distro install debian --override-alias debian-code
	5. backup 备份的命令
	![](Screenshot_2025-05-05-01-48-42-500_com.termux.png)
	6. 例如 proot-distro backup [镜像名字] --output 路径