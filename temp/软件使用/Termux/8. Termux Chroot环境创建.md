1. 创建日期：2025-09-15
2. 测试环境：XiaomiPad6 Max，Termux，Debian
3. 参考教程：
	1. [Geimi](2025-09-15-chroot.ai.md)
	2. https://ivonblog.com/posts/termux-chroot-ubuntu/
	3. https://www.debian.org/releases/stable/arm64/apds03.zh-cn.html
4. 必要前提：
	1. 已经授予Termux root权限
	2. 管理root权限
		1. 直接使用 su 命令进入 root 权限，或者下载 tsu 或者 sudo 软件包。
			1. 下载tsu需要先下载root-repo：
				```bash
				pkg install root-repo
				pkg install tsu
				```
			2. 如果使用 sudo 需要安装：
			```bash
			pkg install sudo
			```
	3. 获取想要的系统的rootfs根目录
		1. 自己制作（Debian参考：[7. 制作arm64 Debian Rootfs文件系统](assert\7.%20制作arm64%20Debian%20Rootfs文件系统.md)）
			对于自己制作的Debian还需要进行环境的初始化配置（第二阶段）
			```bash
			chroot $DEBIANPATH /debootstrap/debootstrap --second-stage
			```
		2. 下载官方或者社区提供的版本（对于Debian来说，proot-distro提供了一个精简的arm64的debian发行版rootfs，在[这里](https://github.com/termux/proot-distro/releases)可以找到）
			解压的时候以root身份解压
5. 对之前已经下载和解包好的rootfs进行进一步的配置，并且完全部署在目标机器上。
	1. 登入tsu进行挂载
		```bash
		# root环境
		tsu
		
		# Debian chroot 文件系统所在路径
		DEBIANPATH="/data/data/com.termux/files/home/debian-rootfs"
		
		# 执行挂载
		mount --bind /dev "${DEBIANPATH}/dev"
		mount -t devpts devpts "${DEBIANPATH}/dev/pts"
		mount -t proc proc "${DEBIANPATH}/proc"
		mount -t sysfs sysfs "${DEBIANPATH}/sys"
		
		# 取消挂载
		umount -R "${DEBIANPATH}/sys" 
		umount -R "${DEBIANPATH}/proc" 
		umount -R "${DEBIANPATH}/dev/pts" 
		umount -R "${DEBIANPATH}/dev"
		```
