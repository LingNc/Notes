1. 简单粗暴的方式，在 .bashrc 中对环境变量 PATH 值进行排除
2. 将以下内容添加到 .bashrc 中
```bash
# 使用 sed 删除路径（支持路径中的斜杠）
export PATH=$(echo "$PATH" | sed 's#:/data/data/com.termux/files/usr/bin##')
```
3. 使用 env -i + [指令]的形式，可以使用干净的变量进入容器